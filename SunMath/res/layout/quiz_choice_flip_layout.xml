<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:padding="10dp">

    <TextView android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:layout_alignParentTop="true"
              android:id="@+id/quiz_choice_flipper_child_title"
              android:textSize="22sp"
              android:layout_marginBottom="10dp"
              android:text="小测试１"
            />


    <WebView android:layout_height="fill_parent"
             android:layout_width="fill_parent"
             android:layout_below="@id/quiz_choice_flipper_child_title"
             android:id="@+id/quiz_choice_flipper_child_question"
             android:visibility="invisible"
            />

    <ProgressBar android:layout_height="wrap_content"
                 android:layout_width="wrap_content"
                 android:layout_centerInParent="true"
                 android:id="@+id/quiz_choice_progress_bar"
                 />

</RelativeLayout>